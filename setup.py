import setuptools

setuptools.setup(
    name="map_helpers",
    version="0.0.1",
    description="helper functions for mapping",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)
